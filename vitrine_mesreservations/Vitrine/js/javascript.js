var btnLogin = document.getElementById('btn-login');

// Suppression des anciennes options si elles existent déjà
var oldOptionsContainer = document.querySelector('.options-container');
if (oldOptionsContainer) {
    oldOptionsContainer.parentNode.removeChild(oldOptionsContainer);
}

// Ajout du conteneur d'options à l'élément parent du bouton de connexion
this.parentNode.appendChild(optionsContainer);

// Initialisation du calendrier FullCalendar
document.addEventListener('DOMContentLoaded', function () {
    // Définition des événements de covoiturage

});


